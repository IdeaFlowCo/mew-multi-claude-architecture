First, navigate in your terminal to the appropriate claude you want to run. 

Currently only claude explorer is implemented, but in the future you should only run claude conductor, which will run sub-claudes.
claude --dangerously-skip-permissions --mcp-config mcp-config.json







TODO: edit below instructions once claude conductor system implemented. will make a subfolder for each claude and leave the readme explaining you probably just want to run claude conductor.

To run claude conductor: [not yet implemented]
claude --file claude-conductor.md --dangerously-skip-permissions --mcp-config mcp-config.json

To run claude explorer:
claude --file claude-explorer.md --dangerously-skip-permissions --mcp-config mcp-config.json

To run claude curator: [not yet implemented]
claude --file claude-curator.md --dangerously-skip-permissions --mcp-config mcp-config.json